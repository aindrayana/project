    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <div class="navbar-header">
          <%= link_to "the sound of life", root_path, class: "navbar-brand lobster" %>
        </div>
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
          <ul class="nav navbar-nav">
            <li class="active"><%= link_to "HOME", root_path %></li>
            <li><%= link_to "About", about_path %></li>
            <li><%= link_to "Blog", blogs_path %>
          </ul>
          <!-- #search navbar -->
          <%= form_tag search_path, class: "navbar-form navbar-left", role: "search" do %>
            <div class="form-group">
              <%= text_field_tag(:search, nil, class: "form-control", placeholder: "search blog ...") %>
            </div>
            <%= submit_tag("search", class: "btn btn-default") %>
          <% end %>
          <!-- # -->

          <ul class="nav navbar-nav navbar-right">
            <% if user_signed_in? %>
              <li class="active"><a href="#">Log in as <%= current_user.first_name %></a></li>
              <li><%= link_to "Log Out", sessions_path, method: :delete %></li>
            <% else %>
              <li><%= link_to "Sign In", new_session_path %></li>
              <!-- <li><%#= link_to "Sign Up", new_user_path %></li> -->
            <% end %>

            <% if @post %>
              <li class="active"><%= link_to 'Create a new post', new_post_path %></li>
            <% else %>
              <li><%= link_to 'Blog Control Panel', posts_path %></li>
            <% end %>
          </ul>
        </div><!-- /.navbar-collapse -->
      </div><!-- /.container-fluid -->
    </nav>
